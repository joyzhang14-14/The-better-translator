# Discord Bot Configuration
DISCORD_TOKEN=your_discord_bot_token_here

# OpenAI API Configuration (use either OPENAI_KEY or OPENAI_API_KEY)
OPENAI_KEY=your_openai_api_key_here
# OPENAI_API_KEY=your_openai_api_key_here  # Alternative name

# Railway will automatically set PORT
# PORT=3000

# Storage Configuration (optional - for persistent dictionary & abbreviations storage)
# STORAGE_TYPE=file  # 'file' for local storage (default), 'url' for cloud storage
# STORAGE_URL=https://api.jsonbin.io/v3/b  # JSONBin API URL example
# STORAGE_TOKEN=your_jsonbin_api_key  # Authentication token for cloud storage
#
# What gets stored in cloud:
# - dictionary.json (translations added by !addprompt)
# - abbreviations.json (shortcuts added by !addabbr, includes default group)
# 
# What stays local:
# - passthrough.json (security sensitive, not stored in cloud)